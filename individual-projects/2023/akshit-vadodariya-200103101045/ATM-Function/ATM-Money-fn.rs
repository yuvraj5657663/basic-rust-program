let notes = [10, 10, 10];

fn withdrow(x, y, z){
    return new [notes[0] - x, notes[1] - y, notes[2] - z];
}

fn deposit(a, b, c) {
    return add = [notes[0] + a, notes[1] + b, notes[2] + c];
}

fn main() {
    notes = withdrow(2, 5, 2);
    notes = deposit(6, 0, 5);
}