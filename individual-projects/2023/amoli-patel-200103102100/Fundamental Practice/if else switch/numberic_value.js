// Write a code to check keyboard has numeric keypad or not.
function main() {
    let input_value = 1 // user input
    if (input_value>=0 && input_value<=9) {
        console.log(input_value, 'nuberic value')
    } else {
        console.log(input_value, 'not a numberic value')
    }
}
main()
